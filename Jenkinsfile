pipeline {
  agent {
    // using dockerfile defined
    dockerfile {
        dir 'jenkins_agents/syntax_agent/'
    }
  }
  stages {
    stage('Testingtesting') {
      steps {
        sh 'echo "koko"'
      }
    }
  }
}
